prepareTreeCov <- function(cohortData,
                           pixelGroupMap,
                           biomassMap){
  browser() # Have this function somewhere already!!
  # Need to rescale all tree layers using raster::rescale(lays) 
  # (accepting all defaults of rescaling and centering)
}